x := 2;
y := x + 1;
x := x + y;
WRITELN((x * y) mod 2);
IF (1 < 2) THEN
BEGIN
    WRITELN(6*9);
    WRITELN((1 < 2) OR (2 < 1));
    WRITELN(NOT (1 > 2));
END;
ELSE
    WRITELN(2);

// READLN(x);
// WRITELN(x+1);

sum := 0;
x := 2;
FOR i := x-1 TO x+1 DO
BEGIN
    sum := sum + i;
    WRITELN(sum);
END;

// WRITELN(-TRUE);
// WRITELN(-1 + 2 AND TRUE);
WRITELN('hello world');
.